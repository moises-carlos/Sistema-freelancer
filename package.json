{
  "name": "sistema-freelancer-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Orquestrador para o backend e frontend do sistema de freelancer.",
  "scripts": {
    "install:backend": "cd back && npm install",
    "install:frontend": "cd interface && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "dev:backend": "cd back && npm run dev",
    "dev:frontend": "cd interface && npm run dev",
    "test:backend": "cd back && npm test"
  },
  "author": "",
  "license": "ISC"
}
